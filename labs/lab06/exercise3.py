# Asks the user to input their full name
student_name = input("Enter your full name: ")

# Displays the name in uppercase
print(f"Uppercase: {student_name.upper()}")

# Displays the name in lowercase
print(f"Lowercase: {student_name.lower()}")

# Displays the length of their name
print(f"Name length: {len(student_name)}")